# Create a directory for native stubs
mkdir -p ~/.wine/drive_c/windows/system32/stubs

# Set override to also look here
wine reg add "HKCU\Software\Wine\DllOverrides" /v "*hcnet*" /t REG_SZ /d "native,builtin" /f

# Restart the session to reset the library handles
wineboot -u