# Check root disk space - should still have ~15GB free
df -h /

# Check that bind mounts are active
mount | grep -E "cache|log|snap|opt|docker" | wc -l
# Should show 5 lines

# Quick test of snapped apps (they should work normally)
snap list 2>/dev/null | head -3