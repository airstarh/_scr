I have databases listed in bash variable:
```
export ALINA_BASES=(
    "alina"
    "stage"
    "vov"
    "m45a"
)
```
Each database is on the same server and has the same data structure.
Each database has table voc with structure and data example as below:
```
id|from   |en_US     |ru_RU |
--+-------+----------+------+
 1|sewa   |Super Star|Писева|
 2|project|          |проект|
 3|Alina  |          |Алина |
 4|admin  |          |админ |

 ```
Field voc.from - unique.
id - primary key.
Admin, ADMIN, admin - are different words.

 I want merge data.
 Main database - alina.
 Slave database - stage (then I will change this to merge data from other databases).
 So all rows must be merged in alina.voc by  unique field `from`.
 For example: if alina.voc.from row has only ru_RU and stage.voc.from has the same `from` but with filled en_US,  then in result I need one unique alina.voc.from with both ru_RU and en_US filled.
 If both `from`s have different ru_RU or en_US, then alina has priority.
 If alina.voc.from does not exist, then it is added from stage.voc.from is it is in slave table.


Please no need to explain much, just MySQL query that I execute in DBeaver connected to one server with those different databases.